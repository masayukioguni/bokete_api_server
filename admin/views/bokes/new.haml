%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:bokes, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:bokes, :new)

.tabs-content
  -form_for :boke, url(:bokes, :create), :class => 'form-horizontal' do |f|
    =partial 'bokes/form', :locals => { :f => f }
